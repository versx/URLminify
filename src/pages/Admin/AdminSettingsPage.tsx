import React from 'react';
import {
  Container,
  Typography,
} from '@mui/material';
export const AdminSettingsPage = () => {
  return (
    <Container>
      <Typography variant="h4" gutterBottom>
        Admin - Settings
      </Typography>
    </Container>
  );
};