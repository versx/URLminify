import {
  Container,
  Typography,
} from '@mui/material';

export const AdminDashboardPage = () => {
  return (
    <Container>
      <Typography variant="h3" gutterBottom>
        Admin - Dashboard
      </Typography>
    </Container>
  );
};